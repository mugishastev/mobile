// This file was previously used for context, but files in the `app/` folder
// are treated as routes by Expo Router. The context has been moved to `/context/AuthContext.tsx`.
// Keeping a minimal default export here to satisfy the router and avoid warnings.
import React from 'react';

export default function AuthContextRoutePlaceholder() {
  return null;
}
